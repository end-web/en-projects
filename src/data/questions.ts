import type { Question } from "../types/practice";

// Local question bank is intentionally empty.
// The app now relies on remote-only data sources.
export const localQuestions: Question[] = [];
export const questions: Question[] = [];
